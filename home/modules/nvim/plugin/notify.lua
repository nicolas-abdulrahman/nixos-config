require("notify").setup({
	-- Customize the notification window (optional)
	timeout = 5000, -- Longer timeout for long messages
	max_width = 80, -- Adjust the width of the notification window
	-- render = "minimal", -- Use a minimal rendering style
})
